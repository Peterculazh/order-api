const product = require('./ProductController');
const bill = require('./BillController');
const user = require('./UserController');
const order = require('./OrderController');

module.exports = {
    product,
    bill,
    user,
    order
}